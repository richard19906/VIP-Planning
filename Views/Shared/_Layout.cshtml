<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>VIP Planning</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
    <style>
        body { 
            background: linear-gradient(rgba(0,0,0,0.7), rgba(0,0,0,0.7)), url('/images/glitter_bg.png') no-repeat center center fixed !important; 
            background-size: cover !important; 
            color: #D4AF37 !important; 
        }
        .sidebar { background: rgba(0, 0, 0, 0.95) !important; border-right: 2px solid #D4AF37 !important; height: 100vh; position: fixed; width: 250px; }
        .main-content { margin-left: 250px; padding: 30px; min-height: 100vh; }
        .nav-link, h1, h2, h3, h4, label, td, th, span { color: #D4AF37 !important; font-weight: bold; }
        .nav-link:hover { color: #fff !important; }
        .btn-gold { background: #D4AF37 !important; color: #000 !important; font-weight: bold; border: none; }
    </style>
</head>
<body>
    <div class="sidebar text-center">
        <h2 class="py-4">V.I.P</h2>
        <nav class="nav flex-column px-3">
            <a class="nav-link" href="/Home/Index">OVERZICHT</a>
            <a class="nav-link" href="/Home/Planning">PLANNING</a>
            <a class="nav-link" href="/Home/Werknemers">PERSONEEL</a>
            <a class="nav-link" href="/Home/Instellingen">INSTELLINGEN</a>
            <a class="nav-link" href="/Home/Instellingen" style="font-size: 0.7rem; padding-left: 20px;">└ PINCODE WIJZIGEN</a>
            <a class="nav-link text-danger mt-5" href="/Account/Logout">UITLOGGEN</a>
        </nav>
    </div>
    <div class="main-content">@RenderBody()</div>
</body>
</html>